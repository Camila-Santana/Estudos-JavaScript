var name = 'Saymon'
var notaSaymon = 10

var name = 'Camila'
var notaCamila = 10

var name = 'Cris'
var notaCris = 10

media = (notaSaymon + notaCamila + notaCris) / 3

//condicao com operador lógico MAIOR e MENOR//

if (media < 8) {
    console.log('Aprovado')
} else {
    console.log('Reprovado')
}

//IF significa SE//
//ELSE significa SENÃO, ou seja, SE NÃO FOR o que eu determinei no IF, o computador vai executar logo o comando else//
// o ELSE é negação do IF//
// NÃO PRRECISA escrever uma condição(código) para o ELSE, ele é automaticamente executado, CASO a condição IF não seja atendida//